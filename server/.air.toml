root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main.exe ./cmd/server"
bin = "tmp/main.exe"
include_ext = ["go", "yaml"]
exclude_dir = ["tmp", "logs", "uploads"]
delay = 1000
stop_on_error = true

[log]
time = true