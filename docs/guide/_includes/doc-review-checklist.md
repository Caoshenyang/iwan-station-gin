> [!IMPORTANT]
> 发布前至少完成这 5 件事：补 `title/description`、检查页内跳转、确认代码可运行、补一处“读者最容易犯错”的提示、执行一次 `pnpm docs:build`。

> [!TIP]
> 如果一页内容超过 8 屏，默认要考虑加入 `[[toc]]`、`details` 折叠块或代码分组，避免读者被“整页信息墙”击退。

> [!WARNING]
> 不要为了炫技而堆扩展。任何扩展都必须服务于一个目标：更快看懂、更少迷路、更愿意继续往下读。
